import React from 'react';

const ScannerPanel = () => {
  return (
    <div style={{ color: '#94a3b8', fontSize: '0.9rem', padding: '12px' }}>
      Scanner capabilities currently polling via yfinance... 
      <br/><br/>
      (MVP placeholder: Waiting for multi-symbol aggregation signals)
    </div>
  );
};

export default ScannerPanel;
