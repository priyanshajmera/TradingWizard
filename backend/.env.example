# Server Environment Configuration
PORT=3001
HOST=0.0.0.0
ENVIRONMENT=development

# Allowed CORS Origins (comma-separated origins or * for open access)
# Examples:
# CORS_ORIGINS=*
# CORS_ORIGINS=http://localhost:5173,https://myfrontend.vercel.app,https://mycustomdomain.com
CORS_ORIGINS=*
